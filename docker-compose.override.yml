version: '3.3'

services:

  api:
    build: ./
    restart: always
    command: uvicorn app.main:app --workers 4 --host 0.0.0.0 --port 80 --reload
    volumes:
      - ./:/usr/src/app:ro
    ports:
      - 80:80 
