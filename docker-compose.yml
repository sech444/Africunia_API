version: '3.3'

services:

  web:
    build: ./
    restart: always
    command: uvicorn app.main:app --workers 4 --host 0.0.0.0 --port 8000
    volumes:
      - .:/usr/src/app
    ports:
      - 80:8000